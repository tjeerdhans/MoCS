@model List<string>
@{
    var selectList = ViewData["selectList"] as SelectList;
}
    @Html.ListBox("", selectList, new { @class = "select2" })

<script>
    $(function () {
        $(".select2").select2({ containerCss: { 'width': '100%' } });
    });
</script>
